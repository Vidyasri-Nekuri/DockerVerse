version: '3'

services:

  frontend:
    image: nginx
    ports:
      - "8080:80"
    volumes:
      - .:/usr/share/nginx/html

  backend:
    build: ./backend
    ports:
      - "5000:5000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
